[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "proteindf_bridge_rs"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
module-name = "proteindf_bridge_rs"
features = ["pyo3/extension-module"]
